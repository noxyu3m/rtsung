require 'rtsung'

rtsung = RTsung::XML.new do
  client 'localhost'
  server '192.168.1.1'

  phase 1, { :rate => 10 }

  option :user_agent do
    user_agent 'Mozilla'
  end

  session :index do
    request '/'
  end
end

print rtsung.to_xml
